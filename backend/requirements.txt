# SIEM Backend Dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy[asyncio]==2.0.32
alembic==1.13.2
aiosqlite==0.20.0
# asyncpg — only needed for PostgreSQL (install separately in production)

# Authentication
PyJWT==2.9.0
bcrypt==4.2.0
python-multipart==0.0.9

# Data Processing (use pre-built wheels)
pandas>=2.1.0
numpy>=1.26.0

# Machine Learning
scikit-learn>=1.5.0
joblib>=1.4.0

# Redis (optional — only needed for production pub/sub)
# redis[hiredis]==5.0.8

# Utilities
python-dotenv==1.0.1
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Testing
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
